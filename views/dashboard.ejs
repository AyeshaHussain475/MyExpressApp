<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      integrity="sha512-...."
      crossorigin="anonymous"
    />
    <style>
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }

      th,
      td {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
      }

      th {
        background-color: #f2f2f2;
      }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  </head>
  <body>
    <h1>Home page</h1>

    <table id="productTable">
      <thead>
        <tr>
          <th>Name</th>
          <th>Price</th>
          <th>Quantity</th>
          <th>Type</th>
          <th>ID</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody></tbody>
    </table>

    <script>
      async function deleteProduct(productId) {
        console.log("Deleting product with ID:", productId);

        try {
          const response = await axios.delete(
            `http://localhost:2000/api/delete/${productId}`
          );

          if (response.status === 200) {
            console.log(response.data);
            // Refresh the table after successful deletion
            fetchProductDetails();
          } else {
            console.error("Error deleting product:", response.data);
          }
        } catch (error) {
          console.error("Error deleting product:", error);
        }
      }

      function fetchProductDetails() {
        axios
          .get("http://localhost:2000/api/getProducts")
          .then((response) => {
            const products = response.data;
            const tableBody = document.querySelector("#productTable tbody");
            tableBody.innerHTML = "";

            products.forEach((product) => {
              const row = document.createElement("tr");
              row.innerHTML = `
              <td>${product.name}</td>
              <td>$${product.price.toFixed(2)}</td>
              <td>${product.quantity}</td>
              <td>${product.type}</td>
              <td>${product._id}</td>
              <td>
                <i class="fas fa-edit" style="color: blue; cursor: pointer;" title="Edit"></i>
                <i class="fas fa-trash-alt" style="color: red; cursor: pointer; margin-left: 10px;" title="Delete" onclick="deleteProduct('${
                  product._id
                }')"></i>
              </td>
            `;
              tableBody.appendChild(row);
            });
          })
          .catch((error) =>
            console.error("Error fetching product details:", error)
          );
      }

      fetchProductDetails();
    </script>
  </body>
</html> -->
